"""
SharePoint (Microsoft Graph) provider — placeholder.

Next steps:
- Implement auth with MSAL (client credentials or device code)
- Resolve site/drive/root folder
- Recursively list driveItems and map metadata to the explorer's item schema
"""

from __future__ import annotations


class SharePointExplorer:
    def __init__(self, *args, **kwargs) -> None:
        raise NotImplementedError("SharePointExplorer not implemented yet")
